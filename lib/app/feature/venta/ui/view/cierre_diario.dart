import 'package:flutter/material.dart';

class CierreDiario extends StatelessWidget {
  const CierreDiario({super.key});
  static const routeName = '/cierre_diario';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
