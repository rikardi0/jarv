import 'package:flutter/material.dart';

class TiendaSettingsView extends StatelessWidget {
  const TiendaSettingsView({Key? key}) : super(key: key);
  static const routeName = '/tienda_settings';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
