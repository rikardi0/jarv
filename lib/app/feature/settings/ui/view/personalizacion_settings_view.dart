import 'package:flutter/material.dart';

class PersonalizacionSettingsView extends StatelessWidget {
  const PersonalizacionSettingsView({Key? key}) : super(key: key);
  static const routeName = '/personalizacion_settings';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
