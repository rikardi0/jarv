import 'package:flutter/material.dart';

class ExportarSettingsView extends StatelessWidget {
  const ExportarSettingsView({Key? key}) : super(key: key);
  static const routeName = '/exportar_settings';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
