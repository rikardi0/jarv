import 'package:flutter/material.dart';

class TimeSettingsView extends StatelessWidget {
  const TimeSettingsView({Key? key}) : super(key: key);
  static const routeName = '/time_settings';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
