import 'package:flutter/material.dart';

class DispositivosSettingsView extends StatelessWidget {
  const DispositivosSettingsView({Key? key}) : super(key: key);
  static const routeName = '/dispositivos_settings';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
