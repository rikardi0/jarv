import 'package:flutter/material.dart';

class EmpresaSettingsView extends StatelessWidget {
  const EmpresaSettingsView({Key? key}) : super(key: key);
  static const routeName = '/empresa_settings';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
