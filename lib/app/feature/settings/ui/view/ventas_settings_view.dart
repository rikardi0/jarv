import 'package:flutter/material.dart';

class VentasSettingsView extends StatelessWidget {
  const VentasSettingsView({Key? key}) : super(key: key);
  static const routeName = '/ventas_settings';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
