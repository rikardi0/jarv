import 'package:flutter/material.dart';

class ProveedorRegistro extends StatelessWidget {
  const ProveedorRegistro({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('data');
  }
}
