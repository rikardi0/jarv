import 'package:flutter/material.dart';

class ProductoRegistro extends StatelessWidget {
  const ProductoRegistro({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('data');
  }
}
