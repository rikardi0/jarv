import 'package:flutter/material.dart';

class TicketDiario extends StatelessWidget {
  const TicketDiario({super.key});
  static const routeName = '/ticket_diario';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
